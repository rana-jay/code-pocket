import { CounrtyCodePipe } from './counrty-code.pipe';

describe('CounrtyCodePipe', () => {
  it('create an instance', () => {
    const pipe = new CounrtyCodePipe();
    expect(pipe).toBeTruthy();
  });
});
